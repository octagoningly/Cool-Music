name: Disabled Keystore Recovery

on:
  workflow_dispatch:

jobs:
  recovery-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Fail with secure recovery guidance
        run: |
          echo "Keystore recovery via GitHub Actions has been disabled."
          echo "Please restore release signing material through an offline,"
          echo "audited process outside this repository."
          exit 1
