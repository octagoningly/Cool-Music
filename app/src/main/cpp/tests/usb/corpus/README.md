# USB Host Test Corpus

The deterministic corpus seed is `324508639`.

Real feedback traces must not be invented from application logs. A trace can be
added only with its source device fingerprint, USB speed, endpoint cadence,
capture method, anonymization record, and SHA-256 documented in the ignored
private evidence tree.
