# USB Host Test Fixtures

Only synthetic descriptor fixtures with explicitly fictional identities belong
here. Device-derived descriptors, fingerprints, names, VID/PID pairs, and traces
remain in the ignored private evidence tree even after anonymization.
