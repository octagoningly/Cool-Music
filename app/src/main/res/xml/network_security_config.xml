<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- listen together accepts user-hosted HTTP servers, so cleartext must stay enabled -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
