# Cool Music 开发规则

> 给 AI / 协作者的**约定总册**。上传、打 Release、改包名、改 Git 流程前先读这份。  
> 与 CONTRIBUTING / README 冲突时，**本文件优先**（尤其：发行、签名、版本号、版本控制、ADB）。

---

## UI 风格

> 界面控件、弹窗、图标、字体的统一约定。改 UI 前先读本节；与单个页面临时样式冲突时，**以本节为准**。

### 圆角与形状

| 控件 | 规格 |
|------|------|
| 筛选 Chip / 胶囊按钮 | `RoundedCornerShape(50)`（全圆胶囊） |
| 探索搜索类型图标钮 | 统一 **48×48**，圆角 **18.dp**，图标 20.dp |
| 下拉菜单 / 弹窗面板 | 圆角 **20–28.dp**（菜单 20，对话框/面板 28），禁止直角 |
| 歌手/选项下拉 | 宽度自适应内容，`widthIn(max = 240.dp)`，长名省略号 |
| 下拉菜单文字 | **居中**（`textAlign = TextAlign.Center`） |
| 搜索条 / 次要圆角 | 16–24.dp，同一屏内保持一致 |

### 图标

- 目标：**简洁、圆角、加粗**。线性图标描边约 **2.2–3.4**，`stroke-linecap/linejoin = round`。
- 播放/上一曲/下一曲/暂停优先 **Apple Music 风格**（圆角三角、胶囊竖条），见 `AppleMusicPlaybackIcons.kt`。
- 品牌图标（B 站/网易/QQ 等）不改圆角与粗细。
- 选中态可用实心，未选中用加粗描边，语义一一对应。

### 字体

- 大标题（首页 Cool Music、探索、媒体库、设置及底栏文案）：**FontFamily.Serif + FontWeight.Bold**。
- 正文保持系统默认，避免整页衬线。

### 交互

- 底栏「首页 / 探索 / 媒体库」**再次点击**当前 Tab：关闭详情并回到列表顶部（设置 Tab 保持原逻辑）。
- 探索页搜索类型按钮层：**向下滚动时收起**，回到顶部展开。
- 歌曲「…」菜单结构统一：播放（保留队列）→ 接下来播放 → 加入队列 → 添加到歌单 → 收藏 →（下载）→ 复制信息。
- 加入歌单需反馈：**「已添加到『歌单名』」** / **「已存在，未重复添加」**。

### 透明材质（高级模糊）

- 同一透明/毛玻璃控件必须走 `AdvancedGlassSurface` + 合适的 `AdvancedGlassRole`，**不要**另写一套 blur。
- 开关：设置 → 动效 → **高级模糊效果**（`advancedBlurEnabled` / 增强 `enhancedAdvancedBlurEnabled`）。
- 模糊强度：设置里的 **模糊度**（`enhancedAdvancedBlurRadiusDp`），经 `AdvancedGlassController.normalizedBlurAmountDp` 生效。
- 弹窗/下拉：圆角 + 半透明底 + 细边；模糊开时贴近 MiniPlayer 质感，关时用 `fallbackColor` 可读底。
- 参考实现：`NeriMiniPlayer`（`AdvancedGlassRole.MiniPlayer`）、`NeriBottomBar`（`BottomNavigation`）、`ui/component/playlist/GlassDropdownMenu.kt`。

### 弹窗美化复用配方（圆角 + 高级透明模糊）

> 口令：**「使用这里面的记录来美化弹窗」** → 直接按本节复用 `GlassDropdownMenu`，不要另起炉灶。
> 标准参考实现：`ui/component/playlist/GlassDropdownMenu.kt`。

#### 原理（两层，且必须同坐标系）

```text
主窗口（同一 Window）
├─ 底层 content 捕获层（captureAdvancedGlassBackdrop）
│    在「玻璃区域」内做模糊（RenderEffect / LocalBlur）
└─ 上层玻璃面（AdvancedGlassSurface）
     只画 tint / 边缘，背后透出模糊
     注册 boundsInWindow 进 regionRegistry
```

- 对齐公式：`模糊区域 = 玻璃面.boundsInWindow − 捕获层.positionInWindow`
- 两层都在**主窗口** `boundsInWindow` 体系里；弹窗若在独立 Window，必须把区域换算到主窗口再注册。

#### 弹窗步骤（照抄）

1. **定位**：自定义 `PopupPositionProvider` + `androidx.compose.ui.window.Popup`。
   `calculatePosition()` 的返回值就是弹窗在**父窗口（主窗口）** 的坐标，直接作为 `regionBoundsOverride`，**禁止**用弹窗内 `boundsInWindow` 或屏幕坐标硬换算。
2. **玻璃面**：内容外包 `AdvancedGlassSurface(role = AdvancedGlassRole.PopupMenu, shape = RoundedCornerShape(20.dp), fallbackColor = …, tintColor = surfaceContainerHigh, regionBoundsOverride = 主窗口 Rect)`。
3. **宽度**：外层 `Modifier.width(IntrinsicSize.Max)`，把 `DropdownMenuItem` 的 `fillMaxWidth` 收成「最宽一项」；`Popup` 无限宽时不加会变成屏宽。
4. **高度**：`heightIn(max = 360.dp)` 即可。**禁止** `IntrinsicSize.Max` + `verticalScroll` 同层（无限高度会闪退）；列表滚动交给 Material 菜单自身。
5. **注册时机**：override 就绪后要用 `LaunchedEffect` 补注册；只靠 `onGloballyPositioned` 会在位置不再变化时漏注册。
6. **关闭模糊**：`LocalAdvancedGlassController.isBaseBlurEnabled == false` 时退 `fallbackColor` 实底，保证可读。
7. **调用方**：放在锚点同级 `Box` 内（`BoxScope`），锚点在前、菜单在后。

#### 踩坑清单

| 坑 | 现象 | 正确做法 |
|----|------|----------|
| 弹窗 `boundsInWindow` 当主窗口坐标 | 模糊偏到屏幕左上 | 用 `PopupPositionProvider` 返回值 |
| `popupView.rootView` 当主窗口原点 | 仍偏移 | 根本不要自己换算 |
| `fillMaxWidth` / 不设宽 | 菜单变成屏宽 | `width(IntrinsicSize.Max)` |
| `IntrinsicSize.Max` + `verticalScroll` | 点开闪退 | 只收宽度，不加自建滚动 |
| 只在 `onGloballyPositioned` 注册 | 模糊不出现/错位 | override 变化时 `LaunchedEffect` 补注册 |
| 品牌图标跟改圆角 | Logo 失真 | 品牌图标不动（见上文图标节） |

---

## 按任务速查

| 你说 / 你要做 | 读哪一节 |
|---------------|----------|
| **发行** / 发版 / Release / 上传安装包 / 检查更新 | → [发行相关](#发行相关) |
| **版本控制** / Git / 分支 / 提交 / rebase / worktree | → [版本控制](#版本控制) |
| **签名** / keystore / 覆盖安装 / 证书 | → [签名](#签名) |
| **版本号** / versionCode / versionName / tag | → [版本号](#版本号) |
| **真机调试** / ADB / install-debug / logcat | → [调试与 ADB](#调试与-adb) |
| **本地 APK** 放哪 / dist | → [本地安装包](#本地安装包) |
| **包名** / applicationId / 应用身份 | → [身份信息](#身份信息) |
| 应用内「检查更新」交互 | → [应用内更新](#应用内更新) |
| 让 AI 按口令干活 | → [给 AI 的简短口令版](#给-ai-的简短口令版) |
| 代码/脚本在哪 | → [相关代码与文档](#相关代码与文档) |

---

## 身份信息

> 不要随意改动。

| 项 | 当前值 | 说明 |
|----|--------|------|
| 应用名 | Cool Music | 界面显示名 |
| applicationId | `moe.ouom.coolmusic` | **安装身份**，改了会丢用户数据 |
| namespace / Kotlin 包名 | `moe.ouom.neriplayer` | 可保持不变，不必全库重命名 |
| 上游 | [NeriPlayer](https://github.com/cwuom/NeriPlayer) | GPLv3 衍生版 |
| 本仓库 | [octagoningly/Cool-Music](https://github.com/octagoningly/Cool-Music) | 发行与更新检查都指向这里 |
| GitHub API（应用内更新） | `repos/octagoningly/Cool-Music/releases/latest` | 见 `core/update/GitHubAppUpdateChecker.kt` |

**禁止：**

- 修改 `applicationId`（除非接受所有用户必须卸载重装、登录全丢）
- 与上游 NeriPlayer 混用包名（已拆成 `moe.ouom.coolmusic`，可共存）
- 用另一把签名密钥出「正式包」（用户覆盖安装会失败，必须卸载）

---

## 签名

> 正式发行 **必须始终使用同一把 keystore**。

| 项 | 约定 |
|----|------|
| 本地路径 | `app/neri.jks`（已在 `.gitignore`，**禁止提交仓库**） |
| alias | `key0` |
| 凭据存放 | 本机 `~/.gradle/gradle.properties` 或 GitHub Secrets，**禁止写进仓库文件**（含 `local.properties`） |
| Gradle 属性 | `KEYSTORE_FILE` / `KEYSTORE_PASSWORD` / `KEY_ALIAS` / `KEY_PASSWORD` |

**AI / 自动化打包时：**

1. 确认 `app/neri.jks` 存在（或 Secrets 能解码出同一把）
2. `assembleRelease` 必须走真实签名，不要 `-PallowUnsignedRelease=true` 出给用户装
3. CI Secrets 里的 keystore 必须与用户手机上已装应用的签名一致
4. **离线备份** `neri.jks` + 密码；丢了就无法再发覆盖更新

**签名自检（发版前必做）：**

```text
apksigner verify --print-certs <release.apk>
```

- 证书 DN 应与历史版本一致（当前本地：`CN=Cool Music Local Release`）
- 包内应有签名信息；用户手机不应再出现「没有签名文件 / 缺乏开发者证书」

---

## 版本号

| 字段 | 规则 |
|------|------|
| `versionCode` | **必须单调递增**，精确到**分钟**（Asia/Taipei）。编码：`(年-2020)*100000000 + 月*1000000 + 日*10000 + 时*100 + 分`，例：2026-09-19 15:30 → `609191530`。见 `Common.kt`。同一天可发多版；同一分钟内连发仍可能相同。 |
| `versionName` | 形如 `<git短哈希>.<MMddHHmm>`，例：`187f98b4.09191620` |
| Tag | 发行 tag 用 `v主.次.修订`（如 `v0.2.3`）；一天多版则每版独立 tag，**不要复用** |

**注意：**

- 新构建自然会得到更大的 `versionCode`（按构建时间到分钟）
- 不要用更小的 versionCode 覆盖旧包
- 在 GitHub Release **正文**里写明本次 `versionCode`，供应用内更新判断
- 不能直接用 `yyMMddHHmm`（会超过 Android Int 上限约 2.1e9），故使用上述打包格式
- **同一分钟连发**可能得到相同 `versionCode`：重打包前先看本次 APK 的 `versionCode`；若需强制不同，可用 `-PbuildVersionTimestamp=MMddHHmm`（见 `Common.kt`）把时间戳往后拨一分钟，并确认仍大于线上版本

---

## 发行相关

> 发版、打 Release、上传 APK、发版前检查都看这里。入口：设置 → 关于 → 检查更新。  
> 实现：`GitHubAppUpdateChecker` + `AppUpdateInstaller`。

### Release 必须包含

1. **Tag**：建议 `v主版本.次版本.修订`（如 `v0.2.3`），或包含可识别版本信息的名字  
2. **至少一个 `.apk` Asset**（名字以 `.apk` 结尾）  
3. **正文里写 versionCode**（强烈推荐，最稳妥）：

```text
## Cool Music v0.2.3

versionCode=609221715

### 更新说明
- …
```

（以**本次构建 APK** 的 `versionCode` 为准，不要手写旧值；编码见 [版本号](#版本号)。）

### 版本判断逻辑（便于排错）

应用内更新按下列顺序判断是否「有新版」：

1. Release 正文中的 `versionCode=数字` **大于** 当前应用的 `BuildConfig.VERSION_CODE` → 提示更新  
2. 若正文没有 versionCode，尝试从 APK 文件名中的数字解析：旧格式 8 位 `yyMMddHH`，或新格式分钟级打包码（如 `609191714`）  
3. 若仍解析不到：tag/名称与当前 `versionName` 不同 **且** 存在 `.apk` 资源 → 提示可更新  
4. tag 包含当前 `versionName` → 视为同一版本，提示已最新  

### 标准发版步骤（AI 可按此执行）

```text
1. 功能合并到 main（或确认 main 已是待发内容）
2. 本地/CI：gradle :app:assembleRelease（必须已签名）
3. 校验：
   - aapt dump badging → package: name='moe.ouom.coolmusic'
   - versionCode 比线上大
   - apksigner verify 通过，证书与历史一致
4. 打 tag（如 v0.2.3）并推送
5. 创建 GitHub Release：
   - tag 对应
   - 上传 release APK（可命名 CoolMusic-<versionName>-arm64.apk）
   - 正文写 versionCode=…
6. 用户端：设置 → 关于 → 检查更新 → 下载并安装（覆盖安装，保留登录）
```

### 上传注意

- 只上传 **已签名** 的 release 包，不要传 debug、不要传未签名包  
- 不要把 `neri.jks`、密码、Cookie 放进 Release 或仓库  
- 分发以 **GitHub Release** 为准；本地安装包统一放在 `dist/apk/`（见 [本地安装包](#本地安装包)）  
- 大体积 debug 包（约 50MB+）不要当发行包发  
- **禁止**把 debug APK 上传 GitHub Release；正式发版只走 `assembleRelease`  

### 发版前检查清单（AI 上传前逐项确认）

- [ ] `applicationId` 仍为 `moe.ouom.coolmusic`
- [ ] 使用与线上一致的签名密钥打包
- [ ] `versionCode` 已增大
- [ ] `apksigner verify` 通过
- [ ] GitHub Release 含 `.apk` 资源
- [ ] Release 正文含 `versionCode=具体数字`
- [ ] 更新说明可读
- [ ] 仓库/Release 中无密钥与密码
- [ ] `更新记录.md` 已补充本次变更
- [ ] 提醒用户：覆盖安装即可，无需卸载

---

## 应用内更新

| 项目 | 约定 |
|------|------|
| UI 入口 | 设置 → **关于** → 检查更新 |
| 下载目录 | `cacheDir/app_updates/`（FileProvider 名 `app_updates`） |
| 安装方式 | 系统安装器覆盖安装（`REQUEST_INSTALL_PACKAGES`） |
| 数据 | 同包名 + 同签名覆盖安装 → **登录/歌单/设置保留** |
| 失败兜底 | 对话框提供「打开发行页」→ GitHub Releases 最新页 |

用户若卸载或换签名重装，登录会丢——这是系统行为，不是 bug。  
本地反复 `installDebug` **不会**在 GitHub 产生 Release/Tag；只有显式创建 GitHub Release 才会对外发版。

---

## 版本控制

> Git 流程、分支、提交信息、worktree、更新记录、禁止提交都看这里。

1. 功能在 **工作分支** 上改，再合入 `main`；不要长期直接在未同步的本地历史上改发行物。紧急文档修正可按维护者明确指示直接改 `main`，常规开发仍走工作分支。
2. **同步 GitHub 推荐流程**（rebase 优先）：

   ```text
   git switch main && git pull origin main
   git switch <工作分支>
   git rebase main
   # 冲突：尽量两边改动都保留；签名 / applicationId / versionCode 规则冲突必须人工确认
   git push origin <工作分支>
   # 合入：PR 或 maintainer 本地 ff-only merge 到 main 后 push
   ```

3. **分支与远端**
   - 分支命名建议：`feat/`、`fix/`、`docs/`、`chore/` + 简短英文（历史分支名可保留）
   - **禁止 force-push `main`**；工作分支整理历史时可 force-push 自己的分支，合入前先与对方确认
   - 合入 `main` 优先 **fast-forward 或 PR merge**，避免无意义的 `Merge branch 'main' into ...` 噪音提交
4. **Submodule**（`np-submodule/*`）：`pull` / `rebase` / `clone` 之后执行  
   `git submodule update --init --recursive`，避免歌词/miuix/LTW 指针漂移。
5. **Worktree（只保留一个）**
   - **本仓只使用主工作树**（仓库根目录本身），**禁止新建** `git worktree add` 旁路目录；并行开发用工作分支，不要多开 worktree
   - 若发现 `git worktree list` 有多条：保留主路径（本机为 `D:\study\AI\NeriPlayer`），其余手动删目录后 `git worktree prune`
   - 本仓含 submodule，**不要**依赖 `git worktree remove`（会报 `working trees containing submodules cannot be moved or removed`）；路径过长删不掉时用 `Remove-Item "\\?\路径"` 前缀
   - 多 worktree 会共享 `.git`，导致 rebase/切分支互相干扰，已踩过坑，不要再开
6. **更新记录**：每次功能完成后更新根目录 `更新记录.md`，新条目**插在标题 `# 更新记录` 正下方**（新在上），格式：

   ```text
   lrq--简短说明--YYYY-MM-DD
   ```

7. **提交信息风格**：`修复xx功能_n` / `新增xx功能_n`（全仓统一，含 CONTRIBUTING；**不用** Conventional Commits）
8. **禁止提交**（README / CONTRIBUTING / AGENTS 均以此为准）：
   - `*.jks`、`*.keystore`、密码、Token、Cookie、完整配置备份/授权导出
   - `logs*.txt`、`__pycache__/`、`*.pyc`、`*.idsig`、`*signedtmp*`
   - APK / `dist/` 安装包、IDE 本地配置、缓存与临时构建产物
   - 签名口令只放 `~/.gradle/gradle.properties` 或 GitHub Secrets，**禁止写入仓库文件**（含 `local.properties`）

---

## 调试与 ADB

本机 Android SDK 路径：`C:\Users\lrq_0\AndroidSDK`  
ADB：`C:\Users\lrq_0\AndroidSDK\platform-tools\adb.exe`

> 注意：CONTRIBUTING 里大量 “USB” 指 **USB 音频独占 / DAC（UAC）播放**，与 **ADB 调试** 无关。ADB 看本节。

### 手机端准备

1. 开发者选项：设置 → 关于 → 连续点击版本号（或系统提示的入口）多次
2. 打开 **USB 调试**
3. USB 连接电脑，连接方式选 **传输文件 / MTP**（不要仅充电）
4. 手机弹出「允许 USB 调试？」→ **允许**（可勾选始终允许）
5. vivo 等机型如安装被拦：设置里允许「文件管理 / 调试」安装未知应用；开发者选项可开 **USB 安装**

### 标准一键脚本（推荐）

项目脚本：`scripts/install-debug.ps1`

```powershell
cd D:\study\AI\NeriPlayer

# 1) 构建并安装 debug 到 USB 设备
.\scripts\install-debug.ps1

# 2) 无线调试（手机「无线调试」里看 IP:端口）
.\scripts\install-debug.ps1 -Wireless 192.168.1.100:5555

# 3) 已有 debug APK，只安装
.\scripts\install-debug.ps1 -SkipBuild
```

脚本行为：

- 定位 `adb.exe`，设置 `JAVA_HOME` / `ANDROID_HOME`
- `adb devices` 检查是否已授权
- 默认执行 `gradlew :app:installDebug`
- `-SkipBuild` 时安装：`app\build\outputs\apk\debug\app-debug.apk`
- 结束后打印启动 / 日志 / 卸载提示

### 常用 ADB 命令（包名 `moe.ouom.coolmusic`）

```powershell
$adb = "C:\Users\lrq_0\AndroidSDK\platform-tools\adb.exe"

# 设备列表（state 应为 device，不是 unauthorized/offline）
& $adb devices -l

# 安装 / 覆盖安装 debug
& $adb install -r app\build\outputs\apk\release\...   # 换成实际路径
& $adb install -r app\build\outputs\apk\debug\app-debug.apk

# 卸载
& $adb uninstall moe.ouom.coolmusic

# 启动主界面
& $adb shell am start -n moe.ouom.coolmusic/moe.ouom.neriplayer.activity.MainActivity

# 日志（脚本提示的 tag）
& $adb logcat -s NERI-LxMusicSource NERI-PlayerManager
# 或更宽
& $adb logcat | findstr NeriPlayer

# 查看已装包
& $adb shell pm list packages | findstr coolmusic

# 无线连接
& $adb connect 192.168.1.100:5555
& $adb disconnect
```

### AI / 自动化调试约定

1. 优先跑 `scripts/install-debug.ps1`，不要手写一长串未验证命令  
2. 无设备时按脚本提示检查 USB 调试与授权，不要强行重复 install  
3. **日常调试装 debug 包**；**对外分发只发 release 签名包**（见 [签名](#签名)、[发行相关](#发行相关)）  
4. **签名统一策略（本地开发）**：`app/build.gradle.kts` 在 `releaseSigningReady` 为真时，让 **debug 也使用** `app/neri.jks` 的 release 签名。这样 `installDebug` / `install-debug.ps1` 可以直接覆盖手机上已装的正式签名应用，**数据不丢**，无需卸载。密钥不存在时 debug 仍回退到系统 debug 签名。  
5. **禁止把 debug APK 上传到 GitHub Release**。debug 未混淆、体积大、可调试；即使签名与 release 相同，也不能当发行包。正式发版仍只走 `assembleRelease` + [发行相关](#发行相关) 清单。  
6. **versionCode/versionName**：每次构建都会按 [版本号](#版本号) 生成新的递增 versionCode 与 `<git短哈希>.<MMddHHmm>` 版本名。本地覆盖安装后手机上版本号变大是预期行为；对外发版时使用**更新的正式构建**即可。  
7. 包名变更后启动/卸载命令必须用 **当前 applicationId**（`moe.ouom.coolmusic`）  
8. 改 USB **音频插入响应** 时另读 `CONTRIBUTING.md` 与 `UsbDeviceAttachHandling.kt`，与 ADB 无关  

**一句话口令：** 本地 debug 可复用 release 签名以便覆盖安装；GitHub 上永远只放 `assembleRelease` 正式包。

### 常见问题

| 现象 | 处理 |
|------|------|
| `unauthorized` | 手机上点允许 USB 调试；换线/换口 |
| 找不到 adb | 确认 SDK `platform-tools`，或 PATH 里有 adb |
| `INSTALL_FAILED_ALREADY_EXISTS` / 签名冲突 | 卸载旧包再装，或改用同一签名覆盖 |
| 提示已安装同包名应用 | 卸载占用 `moe.ouom.neriplayer`/`moe.ouom.coolmusic` 的旧应用 |
| 无线连不上 | 手机与电脑同一局域网；端口以无线调试界面为准 |

---

## 本地安装包

> 不要堆在仓库根目录。

**约定路径：** `dist/apk/`

| 文件 | 含义 |
|------|------|
| `dist/apk/CoolMusic-latest-arm64-release.apk` | 当前最新 **已签名** 本地安装包（给手机侧载） |
| `app/build/outputs/apk/release/*.apk` | Gradle 原始构建产物 |
| `app/build/outputs/apk/debug/app-debug.apk` | 调试包（ADB 用） |

**规则：**

1. **禁止**把 `CoolMusic-*.apk`、`*.idsig` 等堆在仓库根目录  
2. 本机安装 / 发给手机的包放到 `dist/apk/`，默认名 `CoolMusic-latest-arm64-release.apk`  
3. `dist/` 已在 `.gitignore` 中忽略，**不要 commit** 安装包  
4. 对外分发走 **GitHub Release**，不在仓库根目录放 APK  
5. `scripts/sign-release.ps1` 默认输出到 `dist/apk/CoolMusic-latest-arm64-release.apk`  
6. 及时删除 `*.idsig`、`*signedtmp*`、`*v1-only*` 等中间文件  
7. 需要留历史版本时用版本名另存，例如 `CoolMusic-187f98b4.09191620-arm64.apk`

**构建后拷贝示例：**

```powershell
$src = Get-ChildItem app\build\outputs\apk\release\*.apk |
  Sort-Object LastWriteTime -Descending | Select-Object -First 1
New-Item -ItemType Directory -Force dist\apk | Out-Null
Copy-Item $src.FullName dist\apk\CoolMusic-latest-arm64-release.apk -Force
```

**手机安装包路径：** `D:\study\AI\NeriPlayer\dist\apk\CoolMusic-latest-arm64-release.apk`

---

## 相关代码与文档

| 路径 | 用途 |
|------|------|
| `app/build.gradle.kts` | applicationId、签名、打包 |
| `build-logic/convention/.../Common.kt` | versionCode / versionName 生成 |
| `core/update/GitHubAppUpdateChecker.kt` | 检查更新 |
| `core/update/AppUpdateInstaller.kt` | 下载与安装 |
| `ui/screen/tab/settings/about/SettingsAboutUpdateItem.kt` | 关于页 UI |
| `scripts/sign-release.ps1` | 本地给 APK 补签（默认输出 `dist/apk/`） |
| `scripts/install-debug.ps1` | ADB 一键安装调试包 |
| `dist/apk/` | 本地签名安装包目录（gitignore，勿提交） |
| `AGENTS.md` | AI 入口：任务路由、模块地图 |
| `README.md` | 用户向：功能、安装、快速开始 |
| `CONTRIBUTING.md` | 实现边界、扩展路径、质量护栏 |
| `更新记录.md` | 功能变更流水 |

---

## 给 AI 的简短口令版

让 AI「上传发行版」时：

```text
按项目根目录「开发规则.md」的【发行相关】+【签名】+【版本号】执行发版：
1) applicationId 必须 moe.ouom.coolmusic
2) 必须用同一把 release keystore 签名
3) versionCode 递增，Release 正文写 versionCode=数字
4) 上传已签名 APK 到 GitHub Release
5) 不要提交 jks/密码；完成后更新 更新记录.md
```

让 AI「真机安装调试」时：

```text
按「开发规则.md」的【调试与 ADB】：优先运行 scripts/install-debug.ps1
USB 需已开调试并授权；包名 moe.ouom.coolmusic。
本地已配置 debug 复用 release 签名（neri.jks 就绪时），可直接覆盖安装正式签名应用，无需卸载。
禁止将 debug APK 上传 GitHub Release；正式发版只传 assembleRelease 产物。
```

让 AI「改 Git / 提交 / 同步」时：

```text
按「开发规则.md」的【版本控制】执行：
工作分支 + rebase 优先；禁止 force-push main；只保留一个工作树、禁止 worktree add；
提交信息 修复xx功能_n / 新增xx功能_n；完成后把 lrq--说明--日期 插到 更新记录.md 标题下。
```

按清单做完，用户才能「关于 → 检查更新」一键覆盖安装且不丢登录。
