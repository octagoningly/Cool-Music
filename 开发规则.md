# Cool Music 开发规则

> 给后续 AI / 协作者看的发行与更新约定。上传、打 Release、改包名前先读这份。

---

## 1. 身份信息（不要随意改动）

| 项 | 当前值 | 说明 |
|----|--------|------|
| 应用名 | Cool Music | 界面显示名 |
| applicationId | `moe.ouom.coolmusic` | **安装身份**，改了会丢用户数据 |
| namespace / Kotlin 包名 | `moe.ouom.neriplayer` | 可保持不变，不必全库重命名 |
| 上游 | [NeriPlayer](https://github.com/cwuom/NeriPlayer) | GPLv3 衍生版 |
| 本仓库 | [octagoningly/Cool-Music](https://github.com/octagoningly/Cool-Music) | 发行与更新检查都指向这里 |
| GitHub API（应用内更新） | `repos/octagoningly/Cool-Music/releases/latest` | 见 `core/update/GitHubAppUpdateChecker.kt` |

**禁止：**

- 修改 `applicationId`（除非接受所有用户必须卸载重装、登录全丢）
- 与上游 NeriPlayer 混用包名（已拆成 `moe.ouom.coolmusic`，可共存）
- 用另一把签名密钥出「正式包」（用户覆盖安装会失败，必须卸载）

---

## 2. 签名（最关键）

正式发行 **必须始终使用同一把 keystore**。

| 项 | 约定 |
|----|------|
| 本地路径 | `app/neri.jks`（已在 `.gitignore`，**禁止提交仓库**） |
| alias | `key0` |
| 凭据存放 | 本机 `~/.gradle/gradle.properties` 或 GitHub Secrets，**禁止写进仓库文件** |
| Gradle 属性 | `KEYSTORE_FILE` / `KEYSTORE_PASSWORD` / `KEY_ALIAS` / `KEY_PASSWORD` |

**AI / 自动化打包时：**

1. 确认 `app/neri.jks` 存在（或 Secrets 能解码出同一把）
2. `assembleRelease` 必须走真实签名，不要 `-PallowUnsignedRelease=true` 出给用户装
3. CI Secrets 里的 keystore 必须与用户手机上已装应用的签名一致
4. **离线备份** `neri.jks` + 密码；丢了就无法再发覆盖更新

**签名自检（发版前必做）：**

```text
apksigner verify --print-certs <release.apk>
```

- 证书 DN 应与历史版本一致（当前本地：`CN=Cool Music Local Release`）
- 包内应有签名信息；用户手机不应再出现「没有签名文件 / 缺乏开发者证书」

---

## 3. 版本号

| 字段 | 规则 |
|------|------|
| `versionCode` | **必须单调递增**，已改为**精确到分钟**（Asia/Taipei）。编码：`(年-2020)*100000000 + 月*1000000 + 日*10000 + 时*100 + 分`，例：2026-09-19 15:30 → `609191530`。见 `Common.kt`。同一天可发多版；同一分钟内连发仍可能相同。 |
| `versionName` | 形如 `<git短哈希>.<MMddHHmm>`，例：`187f98b4.09191620` |

**发版时：**

- 新构建自然会得到更大的 `versionCode`（按构建时间到分钟）
- 不要用更小的 versionCode 覆盖旧包
- **一天多个版本**：每版单独打 tag（如 `v0.1.1`、`v0.1.2`），不要复用 tag
- 在 GitHub Release **正文**里写明本次 `versionCode`，供应用内更新判断
- 不能直接用 `yyMMddHHmm`（会超过 Android Int 上限约 2.1e9），故使用上述打包格式

---

## 4. GitHub Release 规范（应用内「检查更新」依赖）

入口：设置 → 关于 → 检查更新  
实现：`GitHubAppUpdateChecker` + `AppUpdateInstaller`

### 4.1 Release 必须包含

1. **Tag**：建议 `v主版本.次版本.修订`（如 `v0.2.0`），或包含可识别版本信息的名字  
2. **至少一个 `.apk` Asset**（名字以 `.apk` 结尾）  
3. **正文里写 versionCode**（强烈推荐，最稳妥）：

```text
## Cool Music v0.2.0

versionCode=609191714

### 更新说明
- …
```

（`609191714` = 2026-09-19 17:14，格式见第 3 节；以**本次构建 APK** 的 `versionCode` 为准，不要手写旧值。）

### 4.2 版本判断逻辑（便于排错）

应用内更新按下列顺序判断是否「有新版」：

1. Release 正文中的 `versionCode=数字` **大于** 当前应用的 `BuildConfig.VERSION_CODE` → 提示更新  
2. 若正文没有 versionCode，尝试从 APK 文件名中的数字解析：旧格式 8 位 `yyMMddHH`，或新格式分钟级打包码（如 `609191714`）  
3. 若仍解析不到：tag/名称与当前 `versionName` 不同 **且** 存在 `.apk` 资源 → 提示可更新  
4. tag 包含当前 `versionName` → 视为同一版本，提示已最新  

### 4.3 标准发版步骤（AI 可按此执行）

```text
1. 功能合并到 main（或确认 main 已是待发内容）
2. 本地/CI：gradle :app:assembleRelease（必须已签名）
3. 校验：
   - aapt dump badging → package: name='moe.ouom.coolmusic'
   - versionCode 比线上大
   - apksigner verify 通过，证书与历史一致
4. 打 tag（如 v0.2.0）并推送
5. 创建 GitHub Release：
   - tag 对应
   - 上传 release APK（可命名 CoolMusic-<versionName>-arm64.apk）
   - 正文写 versionCode=…
6. 用户端：设置 → 关于 → 检查更新 → 下载并安装（覆盖安装，保留登录）
```

### 4.4 上传注意

- 只上传 **已签名** 的 release 包，不要传 debug、不要传未签名包  
- 不要把 `neri.jks`、密码、Cookie 放进 Release 或仓库  
- 分发以 **GitHub Release** 为准；本地安装包统一放在 `dist/apk/`（见第 8.6 节）  
- 大体积 debug 包（约 50MB+）不要当发行包发  

---

## 5. 应用内更新体验约定

| 项目 | 约定 |
|------|------|
| UI 入口 | 设置 → **关于** → 检查更新 |
| 下载目录 | `cacheDir/app_updates/`（FileProvider 名 `app_updates`） |
| 安装方式 | 系统安装器覆盖安装（`REQUEST_INSTALL_PACKAGES`） |
| 数据 | 同包名 + 同签名覆盖安装 → **登录/歌单/设置保留** |
| 失败兜底 | 对话框提供「打开发行页」→ GitHub Releases 最新页 |

用户若卸载或换签名重装，登录会丢——这是系统行为，不是 bug。

---

## 6. Git / 协作约定

1. 功能在 **工作分支** 上改，再合入 `main`；不要长期直接在未同步的本地历史上改发行物  
2. 与 GitHub 同步：`main` 拉最新 → 工作分支 rebase/合并 → 处理冲突 → push  
3. 冲突处理：尽量两边改动都保留；**签名、applicationId、versionCode 规则** 冲突时必须人工确认  
4. 每次功能完成后更新根目录 `更新记录.md`，格式：

```text
lrq--简短说明--YYYY-MM-DD
```

5. 提交信息风格：`修复xx功能_n` / `新增xx功能_n`  
6. **禁止提交**：`*.jks`、`*.keystore`、密码、Token、Cookie  

---

## 7. 发版前检查清单（AI 上传前逐项确认）

- [ ] `applicationId` 仍为 `moe.ouom.coolmusic`
- [ ] 使用与线上一致的签名密钥打包
- [ ] `versionCode` 已增大
- [ ] `apksigner verify` 通过
- [ ] GitHub Release 含 `.apk` 资源
- [ ] Release 正文含 `versionCode=具体数字`
- [ ] 更新说明可读
- [ ] 仓库/Release 中无密钥与密码
- [ ] `更新记录.md` 已补充本次变更
- [ ] 提醒用户：覆盖安装即可，无需卸载

---

## 8. USB / ADB 真机调试说明

本机 Android SDK 路径：`C:\Users\lrq_0\AndroidSDK`  
ADB：`C:\Users\lrq_0\AndroidSDK\platform-tools\adb.exe`

> 注意：CONTRIBUTING 里大量 “USB” 指 **USB 音频独占 / DAC（UAC）播放**，与 **ADB 调试** 无关。ADB 看本节。

### 8.1 手机端准备

1. 开发者选项：设置 → 关于 → 连续点击版本号（或系统提示的入口）多次
2. 打开 **USB 调试**
3. USB 连接电脑，连接方式选 **传输文件 / MTP**（不要仅充电）
4. 手机弹出「允许 USB 调试？」→ **允许**（可勾选始终允许）
5. vivo 等机型如安装被拦：设置里允许「文件管理 / 调试」安装未知应用；开发者选项可开 **USB 安装**

### 8.2 标准一键脚本（推荐）

项目脚本：`scripts/install-debug.ps1`

```powershell
cd D:\study\AI\NeriPlayer

# 1) 构建并安装 debug 到 USB 设备
.\scripts\install-debug.ps1

# 2) 无线调试（手机「无线调试」里看 IP:端口）
.\scripts\install-debug.ps1 -Wireless 192.168.1.100:5555

# 3) 已有 debug APK，只安装
.\scripts\install-debug.ps1 -SkipBuild
```

脚本行为：

- 定位 `adb.exe`，设置 `JAVA_HOME` / `ANDROID_HOME`
- `adb devices` 检查是否已授权
- 默认执行 `gradlew :app:installDebug`
- `-SkipBuild` 时安装：`app\build\outputs\apk\debug\app-debug.apk`
- 结束后打印启动 / 日志 / 卸载提示

### 8.3 常用 ADB 命令（包名 `moe.ouom.coolmusic`）

```powershell
$adb = "C:\Users\lrq_0\AndroidSDK\platform-tools\adb.exe"

# 设备列表（state 应为 device，不是 unauthorized/offline）
& $adb devices -l

# 安装 / 覆盖安装 debug
& $adb install -r app\build\outputs\apk\release\...   # 换成实际路径
& $adb install -r app\build\outputs\apk\debug\app-debug.apk

# 卸载
& $adb uninstall moe.ouom.coolmusic

# 启动主界面
& $adb shell am start -n moe.ouom.coolmusic/moe.ouom.neriplayer.activity.MainActivity

# 日志（脚本提示的 tag）
& $adb logcat -s NERI-LxMusicSource NERI-PlayerManager
# 或更宽
& $adb logcat | findstr NeriPlayer

# 查看已装包
& $adb shell pm list packages | findstr coolmusic

# 无线连接
& $adb connect 192.168.1.100:5555
& $adb disconnect
```

### 8.4 AI / 自动化调试约定

1. 优先跑 `scripts/install-debug.ps1`，不要手写一长串未验证命令  
2. 无设备时按脚本提示检查 USB 调试与授权，不要强行重复 install  
3. **调试装 debug 包**；**给用户发 release 签名包**（见第 2、4 节）  
4. 调试包与正式包签名不同，**不能**用 debug 覆盖已装的 release，需先卸载或统一签名  
5. 包名变更后启动/卸载命令必须用 **当前 applicationId**（`moe.ouom.coolmusic`）  
6. 改 USB **音频插入响应** 时另读 `CONTRIBUTING.md` 与 `UsbDeviceAttachHandling.kt`，与 ADB 无关  

### 8.5 常见问题

| 现象 | 处理 |
|------|------|
| `unauthorized` | 手机上点允许 USB 调试；换线/换口 |
| 找不到 adb | 确认 SDK `platform-tools`，或 PATH 里有 adb |
| `INSTALL_FAILED_ALREADY_EXISTS` / 签名冲突 | 卸载旧包再装，或改用同一签名覆盖 |
| 提示已安装同包名应用 | 卸载占用 `moe.ouom.neriplayer`/`moe.ouom.coolmusic` 的旧应用 |
| 无线连不上 | 手机与电脑同一局域网；端口以无线调试界面为准 |

### 8.6 本地安装包目录（不要堆在仓库根目录）

**约定路径：** `dist/apk/`

| 文件 | 含义 |
|------|------|
| `dist/apk/CoolMusic-latest-arm64-release.apk` | 当前最新 **已签名** 本地安装包（给手机侧载） |
| `app/build/outputs/apk/release/*.apk` | Gradle 原始构建产物 |
| `app/build/outputs/apk/debug/app-debug.apk` | 调试包（ADB 用） |

**规则：**

1. **禁止**把 `CoolMusic-*.apk`、`*.idsig` 等堆在仓库根目录  
2. 本机安装 / 发给手机的包放到 `dist/apk/`，默认名 `CoolMusic-latest-arm64-release.apk`  
3. `dist/` 已在 `.gitignore` 中忽略，**不要 commit** 安装包  
4. 对外分发走 **GitHub Release**，不在仓库根目录放 APK  
5. `scripts/sign-release.ps1` 默认输出到 `dist/apk/CoolMusic-latest-arm64-release.apk`  
6. 及时删除 `*.idsig`、`*signedtmp*`、`*v1-only*` 等中间文件  
7. 需要留历史版本时用版本名另存，例如 `CoolMusic-187f98b4.09191620-arm64.apk`

**构建后拷贝示例：**

```powershell
$src = Get-ChildItem app\build\outputs\apk\release\*.apk |
  Sort-Object LastWriteTime -Descending | Select-Object -First 1
New-Item -ItemType Directory -Force dist\apk | Out-Null
Copy-Item $src.FullName dist\apk\CoolMusic-latest-arm64-release.apk -Force
```

**手机安装包路径：** `D:\study\AI\NeriPlayer\dist\apk\CoolMusic-latest-arm64-release.apk`

---

## 9. 相关代码与文档

| 路径 | 用途 |
|------|------|
| `app/build.gradle.kts` | applicationId、签名、打包 |
| `build-logic/convention/.../Common.kt` | versionCode / versionName 生成 |
| `core/update/GitHubAppUpdateChecker.kt` | 检查更新 |
| `core/update/AppUpdateInstaller.kt` | 下载与安装 |
| `ui/screen/tab/settings/about/SettingsAboutUpdateItem.kt` | 关于页 UI |
| `scripts/sign-release.ps1` | 本地给 APK 补签（默认输出 `dist/apk/`） |
| `scripts/install-debug.ps1` | ADB 一键安装调试包 |
| `dist/apk/` | 本地签名安装包目录（gitignore，勿提交） |
| `README.md` | 「开发与真机调试」简要说明 |
| `CONTRIBUTING.md` | 构建、adb install/logcat、Debug 页、USB 音频实现 |
| `更新记录.md` | 功能变更流水 |

---

## 10. 给 AI 的简短口令版

以后让 AI「上传发行版」时，可附带：

```text
按项目根目录「开发规则.md」执行发版：
1) applicationId 必须 moe.ouom.coolmusic
2) 必须用同一把 release keystore 签名
3) versionCode 递增，Release 正文写 versionCode=数字
4) 上传已签名 APK 到 GitHub Release
5) 不要提交 jks/密码；完成后更新 更新记录.md
```

让 AI「真机安装调试」时：

```text
按开发规则.md 第 8 节：优先运行 scripts/install-debug.ps1
USB 需已开调试并授权；包名 moe.ouom.coolmusic；不要用 debug 包覆盖 release 正式包。
```

按清单做完，用户才能「关于 → 检查更新」一键覆盖安装且不丢登录。
